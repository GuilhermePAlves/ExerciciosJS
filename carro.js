const cars = ['Saab', 'Volvo', 'BMW', 'Chevrolet', 'Fiat', 'Ferrari', 'Mitsubish'];

for (let index = 0; index < cars.length; index += 1) {
    // console.log(index)
    // console.log(cars[index]);
    console.log(`${index} - ${cars[index]}`)
};  